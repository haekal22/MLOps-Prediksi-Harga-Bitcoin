global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "btc-fastapi"

    static_configs:
      - targets:
          - "api-service:8000"

rule_files:
  - "prometheus.rules.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]